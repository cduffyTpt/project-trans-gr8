namespace TransGr8_DD_Test.Rules
{
    /// <summary>
    /// The order of enum items, is the order of the rules execution
    /// </summary>
    public enum RulesOrdinal
    {
        LevelRule = 0,
        ComponentRule = 1,
        RangeRule = 2,
        ConcentrationRule = 3
    }
}
